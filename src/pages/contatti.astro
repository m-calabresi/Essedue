---
import PageLayout from "../layouts/PageLayout.astro";

import Heading from "../components/Heading.astro";

import banner from "../assets/webp/contatti-header-lg.webp";
---

<PageLayout
    title="Contatti"
    selectedItem="contatti"
    metaContent=`Contattaci per un preventivo su misura. Siamo ${import.meta.env.ESSEDUE_NAME}, progettiamo e realizziamo soluzioni d'arredo secondo le vostre esigenze per ambienti di ogni stile e dimensione. Doniamo un nuovo aspetto a mobili e arredi utilizzando tecniche di finitura e verniciatura a pennello, realizzate esclusivamente a mano. Ripristiniamo vecchi mobili, donando loro una nuova vita tramite interventi di restauro che preservano l'unicità e ne migliorano la durabilità nel tempo.`>
    <Heading
        title="Contattaci"
        subtitle="Preventivi su misura, che si adattano alle tue esigenze."
        banner={banner}
        imageClass="grayscale object-bottom"
    />
    <main
        class="mx-auto my-32 grid w-full max-w-7xl grid-cols-1 grid-rows-2 items-center justify-center gap-16 px-4 sm:px-20 lg:grid-cols-5 lg:grid-rows-1 lg:gap-4">
        <section
            class="flex flex-col gap-16 px-8 text-start text-lg capitalize text-black lg:col-span-2 lg:pr-0">
            <div class="flex flex-col gap-2">
                <h2 class="font-bold">sede</h2>
                <address class="not-italic">{import.meta.env.ESSEDUE_SHORT_ADDRESS}</address>
            </div>
            <div class="flex flex-col gap-2">
                <h2 class="font-bold">orari</h2>
                <div class="grid grid-cols-2">
                    <span class="row-span-2">lun - ven</span>
                    <span>
                        <time datetime="8:00">08:00</time> - <time datetime="12:00">12:00</time>
                    </span>
                    <span>
                        <time datetime="14:00">&nbsp14:00</time> - <time datetime="18:00">18:00</time>
                    </span>
                </div>
            </div>
            <div class="flex flex-col gap-2">
                <h2 class="font-bold">telefono</h2>
                <address class="grid grid-cols-2 not-italic">
                    <span>{import.meta.env.ESSEDUE_PRIMARY_PHONE}</span>
                    <span>{import.meta.env.ESSEDUE_PRIMARY_NAME}</span>
                    <span>{import.meta.env.ESSEDUE_SECONDARY_PHONE}</span>
                    <span>{import.meta.env.ESSEDUE_SECONDARY_NAME}</span>
                </address>
            </div>
            <div class="flex flex-col gap-2">
                <h2 class="font-bold">email</h2>
                <address class="lowercase not-italic">{import.meta.env.ESSEDUE_EMAIL}</address>
            </div>
        </section>
        <section class="w-full lg:col-span-3 lg:h-full lg:pr-8">
            <iframe
                class="h-[36rem] w-full md:h-[30rem] lg:h-full"
                title="Posizione su Google Maps"
                src="https://maps.google.com/maps?q=Via%20Alessandro%20Manzoni%2022B,%20Carugo%20(CO)&t=&z=17&ie=UTF8&iwloc=&output=embed&z=14"
                allowfullscreen="true"
                loading="lazy"
                referrerpolicy="no-referrer-when-downgrade"
            ></iframe>
        </section>
    </main>
</PageLayout>
